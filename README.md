# Visualizador do Algoritmo de Dijkstra 🔍

Este projeto é uma implementação interativa do Algoritmo de Dijkstra em Python, desenvolvida para a disciplina de Estrutura de Dados Avançada. A aplicação permite gerar grafos aleatórios e visualizar o caminho mínimo entre dois nós.

## 🚀 Funcionalidades

- **Geração de Grafos Aleatórios:** Cria vértices e arestas ponderadas automaticamente.
- **Visualização Gráfica:** Utiliza `matplotlib` e `networkx` para desenhar o grafo.
- **Cálculo de Caminho Mínimo:** Implementação eficiente do Dijkstra usando fila de prioridade (`heapq`).
- **Interface Interativa:** GUI construída com `tkinter` para fácil interação.

## 🛠️ Tecnologias Utilizadas

- Python 3.12+
- Tkinter (Interface Gráfica)
- NetworkX (Manipulação de Grafos)
- Matplotlib (Plotagem e Visualização)

## 📦 Como executar

1. Clone o repositório:

   ```bash
   git clone https://github.com/DanielDaigo/dijkstra-python-gui.git
   ```

2. Instale as dependências:

   ```bash
   pip install -r requirements.txt
   ```

3. Execute a aplicação:
   ```bash
   python app.py
   ```


